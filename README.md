# QuantLab — A股量化研究与回测框架

> 面向初学者的完整量化研究框架：数据 → 因子 → 模型 → 回测 → 模拟盘
>
> 第一次接手先读 `docs/入门.md`；只想看清"这个工作区有什么、从哪进"，看上一级的 `../总览.md`。

## 快速开始

```bash
# 1. 安装依赖
pip install -r requirements.txt

# 2. 一键运行全流程（日期不写 = 2021-01-01 到今天；本机因子面板现为 2021-01-04~2026-09-22）
python main.py pipeline --universe 000852 --start 2021-01-01 --end 2026-09-22

# 3. 或分步执行
python main.py download --universe 000852 --start 2021-01-01 --end 2026-09-22
python main.py factors --start 2021-01-01 --end 2026-09-22
python main.py train                      # Walk-Forward 滚动训练（可用 --min-train-months/--retrain-months 覆盖）
python main.py backtest --capital 1000000
```

## 项目结构

```
QuantLab/
├── main.py               # CLI 入口
├── config.yaml           # 全局配置
├── requirements.txt      # 依赖列表
├── data/                 # 数据层
│   ├── downloader.py     # AKShare 数据下载
│   ├── cleaner.py        # 数据清洗
│   └── cache.py          # Parquet 缓存
├── factors/              # 因子引擎
│   ├── technical.py      # 技术因子（动量、波动率、RSI、MACD等）
│   ├── fundamental.py    # 基本面因子（PE、PB、市值等）
│   ├── processor.py      # 因子处理（缩尾、标准化、中性化）
│   └── evaluator.py      # 因子评估（IC、IR、相关性）
├── models/               # 模型层
│   ├── dataset.py        # 数据集构建（时序交叉验证）
│   ├── trainer.py        # LightGBM 训练（含Optuna调参）
│   └── predictor.py      # 预测信号生成
├── backtest/             # 回测引擎
│   ├── engine.py         # 向量化回测
│   ├── cost.py           # 交易成本（佣金、印花税、滑点）
│   ├── metrics.py        # 绩效指标
│   └── reporter.py       # 报告生成（HTML + 控制台）
├── paper_trade/          # 模拟盘
│   ├── broker.py         # 模拟券商（T+1、涨跌停、整手）
│   ├── portfolio.py      # 持仓跟踪 + 风控
│   └── journal.py        # 交易日志
└── utils/
    ├── calendar.py        # A股交易日历
    ├── position_policy.py # 分数带位状态机（建仓/补仓/减仓/清仓 + 状态持久化）
    ├── exposure.py        # 组合级暴露层（目标波动缩放 + 运行时 IC 门控）
    └── logger.py          # 日志
```

## 核心设计

| 模块 | 关键特性 |
|------|---------|
| **数据** | 腾讯行情直连（前复权）+ akshare/东财两级降级，Parquet本地缓存，8线程并发 |
| **因子** | 25 个技术因子（`factor_panel` 实测列数），缩尾→标准化→滞后防未来函数；基本面默认关闭（实时快照映射历史=未来函数） |
| **模型** | LightGBM 回归预测**未来 5 日**收益率，Walk-Forward 滚动训练（每 6 个月重训，全样本外预测） |
| **回测** | 事件循环引擎，含佣金/印花税/滑点，月度调仓，涨停不买/跌停不卖（按**次日开盘涨跌幅**判，撮合价就是开盘价）/停牌不成交，缺行按最近收盘估值，支持基准指数超额对比 |
| **模拟盘** | T+1规则，100股整手，涨跌停不可交易（板块 10%/20%，与回测共用 `utils/market_rules.py`） |

> **现行默认档实测（`model.horizon: 5`，2026-09-26 两处改动之后复核）** — 同一份
> `data/cache/predictions.parquet` 上的三档对照，100 万本金 / 月度评估 / 887 个交易日，
> Sharpe 用报告口径（rf=2%）。数字是**费后**，印花税按成交日分档（见「交易成本口径」）。
> 本轮两处改动：① 建仓线按要求从 P84.57 提到 **P94**（`buy_score 0.01354` / `strong_score
> 0.01807` = P97，沿用"最前 3% 给满仓"）；② 涨跌停判定从"当日收盘涨跌幅"改成**次日开盘涨跌
> 幅**——撮合价就是开盘价，判定必须看同一个时点（`utils/market_rules.py` 的 `can_fill`）：
>
> | 口径 | 策略累计 | 年化 | 年化波动 | Sharpe | 最大回撤 | 笔数 | 成本占本金 | 平均仓位/只数 |
> |---|---|---|---|---|---|---|---|---|
> | Top-50 等权（`enabled:false`） | +75.21% | 17.27% | 29.31% | 0.52 | 39.23% | 3981 | 13.82% | — |
> | 分数带位 12 只（无暴露层） | +115.34% | 24.35% | 31.89% | 0.70 | 43.81% | 651 | 9.97% | 78.0% / 11.9 |
> | **带位 + 暴露层 A+B（现行默认）** | **+159.22%** | 31.08% | 17.95% | **1.62** | **22.67%** | 306 | 5.20% | 35.9% / 5.0 |
>
> 第三行就是 `python main.py backtest` 打的报告（同一口径的存档在 `reports/verify_results.txt`，
> 指数序列取到 09-22）：Calmar 1.37、信息比率 1.00、日胜率 47.46%、**交易胜率 52.84% / 利润因子
> 2.63**（按笔已实现盈亏，已扣买入成本与双边费用）、基准 `sh000852` 同期 +12.18% → 超额
> +147.04pp、IC 门控触发 8/45 次评估、平均仓位上限 44.8%（最低 14.2%）、期末 0 只。
> 分年（日历年总收益，年内日收益复利，四行相乘 = +159.22% 全期累计）：2023 +1.2%（指数 −13.5%）
> / 2024 +60.5%（+1.2%）/ 2025 +26.7%（+27.5%）/ 2026 +25.9%（+2.2%）—— 只有 2025 跑输指数，
> 差 0.8 个点，其余三年正超额。
>
> 两处改动各自的影响在 `research/fill_and_threshold.py` 里一次只动一个变量地量过
> （`logs/fill_and_threshold.log`）：只换成交判定 +11.7pp（回撤 +5.5pp；机械效果是"买入被挡"
> 从 11 笔降到 1 笔——旧规则把 10 笔"收盘才涨停、开盘根本买得到"的单子错拦了）；只换建仓线
> −3.2pp（回撤 +5.2pp）；两项都改落到 +159.22%。**同一套"免掉一项费率"的噪声探针在两档上
> 分别给出 7.0pp 与 17.4pp 的净值摆动**（见「交易成本口径」末段），所以上面这些差**不能读成
> "变好了"**：改这两处的理由是口径正确（判定看对了时点、分数线按要求落到 P94），不是更高收益；
> −3.2pp 那一档更是整个落在噪声带里。
>
> 把标签从 20 日换成 5 日的理由不是"Sharpe 更高"，是**尾部没那么脆**（剔除对全样本
> 贡献最大的月份，日收益算术年化 / rf=0，两档同一口径；h5 行 2026-09-26 建仓线 P94 +
> 开盘成交判定之后重测，h20 行仍是 09-25 口径、未重跑）：
>
> | 默认档 | 全样本 | 剔 2024 | 剔最好 3 月 | 剔最好 5 月 |
> |---|---|---|---|---|
> | horizon=20（上一版） | +124.2% / 1.32 | +32.3% / 0.73 | +16.2% / 0.36 | **−4.3% / −0.01** |
> | **horizon=5（现行）** | +159.2% / 1.60 | +61.5% / 1.19 | +29.8% / 0.57 | **+1.4% / 0.10** |
>
> h20 那一档去掉最好的 5 个月就转亏（+46.3%/+14.2%/+15.5% 三个月撑起全部收益）；
> h5 剔完仍然是正的。切换的代价是阈值必须按分位重推（见下文「阈值标定」）。
>
> **2026-09-22 那一轮全链路数字（horizon=20）**（桌面机：新下载器重建缓存 → 因子 → 8 折
> Walk-Forward 训练 28 s → `research/verify_backtest.py`），完整表在 `reports/verify_results.txt`，
> `evidence/verify_results.txt` 留作执行层修复**前**的对照存档。
>
> | 区间 | 口径 | 策略累计 | 年化 | 年化波动 | Sharpe | 最大回撤 | 成本 |
> |---|---|---|---|---|---|---|---|
> | 2023-02-01 ~ 2026-09-22（887 日） | Top-50 等权（策略关闭） | +30.95% | 7.96% | 27.11% | 0.220 | 43.16% | 11.51% 本金 |
> | 2023-02-01 ~ 2026-09-22（887 日） | 分数带位 12 只（无暴露层） | +123.16% | 25.62% | 29.63% | 0.797 | 41.12% | 8.86% 本金 |
> | 2023-02-01 ~ 2026-09-22（887 日） | **带位 + 暴露层 A+B（当时的默认档）** | **+121.60%** | 25.36% | 19.08% | **1.224** | **23.21%** | 4.29% 本金 |
> | 2023-02-01 ~ 2025-12-31（与旧存档同区间） | Top-50 等权 | +20.24% | 6.75% | 27.55% | — | 43.16% | — |
> | 2023-02-01 ~ 2025-12-31（同上） | 带位 + 暴露层 A+B | +70.70% | 20.87% | 19.58% | — | 23.21% | — |
>
> 开启 `position_policy` 后前两行旧口径**作废**，只作对照保留。h20 那档分年（**年内首末收盘
> 价相除的旧口径**，那轮缓存已不在、没按新口径重测，与上面 h5 那四分年不可逐值对比）：
> 2023 −8.22% / 2024 +70.95% / 2025 +10.40% / 2026 +29.02%，同期指数 +31.02%、等权组合
> +45.18% —— 2025 那一年这一档把仓位压到 36%，**跑输基准 20 个点**，
> 少下的注在牛市里就是少赚的钱。
>
> 同区间从旧存档的 +54.79% / Sharpe 0.42 掉到 +20.24%：主要是执行层修复
> （差额建仓、无前视盯市、涨停/停牌不可成交）把原先过于乐观的撮合挤掉了，
> 但这一轮模型也在重训（缓存变了、`predictions.parquet` 是新生成的），
> 所以它不是逐值可比的对照实验。
>
> **基准口径纠正**：`research/verify_backtest.py` 过去把"今日 1000 只成分的等权组合"
> 标成"中证1000"来算超额。等权组合同期 +55.72%，于是结论是"跑输 20 多个点"；
> 真实指数 `sh000852` 同期只有 +12.18%（年化 3.32%），换成它以后是超额 **+109.42pp**、
> 信息比率 **0.79**（h20 档；Top-50 口径 IR 0.44，带位无暴露层 1.06）。两个基准现在都印在报告里——在这个股票池上做多，等权组合才是该翻的
> hurdle，但股票池本身用的是**当前**成分，两侧都带幸存者偏差，绝对收益都偏乐观。
> 偏差量级已经量过（`research/survivorship_probe.py` → `logs/survivorship_probe.log`，
> 2026-09-26，44 s）：2023-01-05 前已上市、之后退市的 151 只里 147 只能取到行情，同一窗口
> 买入持有**中位 −87.7%**、只有 3% 为正，而留下的一千只中位 +11.9%；把这 147 只放回
> 等权池，均值从 +58.5% 掉到 +39.3%（折年化 +13.3% → +9.4%，**≈3.9pp/年**）。这是**下界**：
> 只拿得到交易所的终止名单，"当年在指数里、后来被调出"那一整块免费接口取不到。别用
> "反正买不到"给它打折——这批名字中位日成交额 0.39 亿（在册池 1.46 亿），相对本策略
> 9.7 万元的中位单笔只有 1% 买不起一单。股票池一侧的 1000 只在全样本里没有任何一只
> 中途缺席（0/1000）。
> （另修掉一处区间错配：基准原先按 `predictions` 末日裁掉，而净值曲线走到缓存末日，
> 最后约一个月的指数涨幅被抹掉了 3 个百分点。）

## 数据获取（`data/downloader.py`，2026-09-22 重写）

旧路径是 akshare 的 `stock_zh_a_hist_tx`：它按**自然年**一年一个 HTTP 请求，
复用不了连接，仓库里还额外对每只股票 `sleep(0.5+随机)` 串行跑 —— 1000 只约
75 分钟。新路径直连腾讯 `newfqkline`，一次 640 根、按 `end` 向前翻页，
线程池并发 + 全局令牌桶限速：

| | 旧 | 新 |
|---|---|---|
| 1000 只全量 | ≈75 min | **2.3 min**（8 线程 / 20 请求每秒，7.3 只每秒） |
| 12 只 A/B | 24.8 s | **0.7 s**（34.7×） |
| 缓存已覆盖 | 仍发请求 | 0 请求，直接切片 |

其余口径：

- **三级降级**：直连腾讯 → akshare 腾讯 → 东方财富。任一级失败才进下一级，
  每次失败把令牌桶速率减半（有地板），恢复时 ×1.5 不冲过设定值。
- **列与单位**（三条路径写进缓存后完全一致）：`成交量`=股、`成交额`=元、
  `换手率`=小数。两处是实测出来的坑：akshare 把 `sz000` 当指数漏乘 100，
  而 `sz000` 同时命中平安银行/万科这类深市主板股票（实测成交量×价格 vs 成交额
  差 100 倍）；东财 `stock_zh_a_hist` 的成交量是手。修正只动绝对值——项目里
  成交量只用于量比（与自身均量之比）和停牌判定（是否为 0），尺度无关，
  因此不改任何已发布数字。
- **等价性**：7 只股票（含科创板 / 001 号段 / 长历史）与 akshare 逐字段比对，
  maxdiff 全为 0；`tests/test_downloader.py` 24 项离线测试钉住翻页语义、
  单位换算、缓存增量与降级链。
- **基准指数**：`download_index_daily("000852")` 走同一条直连通道（东财指数接口
  在本机 IP 上常年 `RemoteDisconnected`）。指数前缀规则与个股不同，
  000 号段在上海，故单独走 `index_symbol()`，不能让 `tx_symbol()` 把它判成深市股票。

## 分数带位仓位管理（`position_policy`，2026-09-22 起默认开启）

Top-K 等权 50 只的问题：权重与分数无关，第 50 名和第 1 名同仓，月末一次性换手
把全部筹码暴露在同一天的模型误差上。`config.yaml` 的 `position_policy.enabled`
打开后，"该买谁"不再是名单，而是**分数带 + 每只股票自己的建仓基准**的函数
（状态机在 `utils/position_policy.py`，与 quantlab2 同一份实现）：

| 规则 | 常量 | 语义 |
|------|------|------|
| 建仓线 / 清仓线 | `buy_score` / `sell_score` | 分数 ≥ 建仓线才入场；跌破清仓线整只清空 |
| 基准仓 | `base_weight` = 5% | 每笔建仓的绝对市值占比（`normalize=False`，归一化会让"5%"失去含义） |
| 建仓上限 | `max_entry_weight` = 8% | 分数越高首笔越大，到 `strong_score` 给满，封顶 8% |
| 补仓触发 | Δ = `step_multiplier` × (建仓分数 − 建仓线) | 比 `ref_score` 再涨够一个 Δ 才补一档，Δ 冻结在该股状态里 |
| 持仓上限 | `max_position_weight` = 16% | 每档 `add_step_weight`，到 16% 锁死 |
| 减仓 | 带内回落 | 分数下降但未破清仓线 → 每档 `trim_step_weight`，减到低于 `min_hold_weight` 改清仓 |
| 回补禁令 | `trim_price` | 记实际减仓成交价，此后补仓参考价不得高于它（`trim_price_ttl_days` 自然日内有效） |
| 最小笔额 | `min_trade_value` = 10000 | 不足一万不下单，下一轮分数继续走再试 |

链路分工（**只有这三条**实现策略，其余调仓入口在策略打开时会明确报错/告警，
不会静默按等权摊派下单）：

- `python main.py backtest` — `backtest/engine.py` 逐评估日跑同一个状态机，
  摘要里多出"策略动作 / 平均目标仓位 / 平均持仓只数"三行诊断。
- `python live/run_daily.py [--simulate|--qmt]` — 策略模式下**每个交易日**评估
  （月末才看一次会把月内的补/减档全丢掉），每只股票的状态存
  `live/state/policy_state.json`，只在**实际股数发生变化**后推进；涨跌停、停牌、
  资金不足挡下的单子下一轮自动重做。
- `python main.py paper-trade` — 走同一条状态机，但节奏仍是月末（函数按
  `rebalance_dates` 循环），启动时会打警告：看策略表现请用 `backtest`。

⚠ 阈值量纲取决于 `model.type` 与 `model.horizon`：`regressor` 的分数是预测
`horizon` 日收益率（现行 `horizon: 5`，`buy_score: 0.01354` = 预期 +1.35%），
`classifier` 的分数是上涨概率（0~1），默认阈值在分类头下等于不建仓。**改
`model.horizon` 会整体缩放分数分布**（同一个 P84 分位点：h5 实测 0.00875，`logs/calib_stats.log`
§1；h20 那一版当年落在 0.030 附近、旧缓存已不在故无复核值，两者差约 3.4 倍），阈值必须按同一分位数重推——旧值直接搬过去等于人人都过建仓线。改动任何带位参数都会让
既有回测结论作废，需要重跑回测（`predictions.parquet` 本身不变，**不需要重训**）。

## 组合级暴露层（`exposure_overlay`，2026-09-22 起默认开启）

带位状态机只管"单票给多少、名额几个"，`max_total_pct` 是一个写死的数。上面
那张表说明写死的数才是回撤的开关，于是把它变成每轮按市场状态现算的量
（`utils/exposure.py`，与 quantlab2 同一份实现）。两条通道乘在同一个系数上：

| 通道 | 算式 | 打的是哪一段 |
|------|------|------|
| **B 目标波动** | `scale = clip(vol_target_ann / 近 20 日全池等权已实现年化波动, 0.30, 1)`，只降不升 | 高波动段照常满仓 → 波动尖峰期风险翻倍 |
| **A 运行时 IC 门控** | 近 60 日**已兑现**（标签走满 `ic_horizon_days`=5 日）截面 RankIC 均值 < 0 → 上限再 ×0.50，且新仓与补仓全部挂起 | 模型失效还满仓 |

系数落在哪：`policy_plan(..., max_total_pct_override=上限×scale,
entry_allowed=不触发门控)`，override 只能压低基线（`min()` 兜住），清仓出口
永不受限。三条链路（`main.py backtest` / `live/run_daily.py` /
`main.py paper-trade`）都经 `overlay_args()` 这一个翻译点取参数，回测与实盘
不会各算各的。**无前视**：两个量只用信号日收盘及之前的信息，成交在次日开盘；
`at()` 只取观测日 ≤ 评估日 − `ic_horizon_days`（现行 5）个交易日的 RankIC 片段，
`tests/test_exposure.py`
里用"截断历史重算必须逐点相等"钉住这条。波动用全池等权而非组合净值，是因为
开局没有净值历史，而多头组合的波动几乎就是它的波动（β≈1.11、ρ≈0.92）。

**A 通道现行样本里的账是负的**（`logs/h5_arms_p94.log` 的归因档，见下面结论 4(b)）：
只开 B +174.31%，A+B +159.22%，只开 A +76.79% —— A 单独开着连"什么都不加"都不如。
它留在默认档是防御性的（赌"信号失效还满仓"这一段将来会出现），不是样本内买到的东西。
一行 `exposure_overlay.ic_window_days: 0` 就能关掉，不需要重训；本轮未改。

### 阈值标定与全档实测（现行档阈值 2026-09-26 重推到 P94/P97，`data/cache/predictions.parquet` 反推）

分数 = 预测 5 日收益率。样本外 85.5 万行（896 个交易日 × 1000 只）的截面分布：中位
**+0.25%**、均值 +0.27%、σ 0.89%、P90 +1.09%、P94 **+1.35%**、P97 **+1.81%**、P99 +2.89%
（`research/calib_stats.py`，输出 `logs/calib_stats.log`）。按每日分数名次分桶，
组内**实际**前向收益：

| 分桶 | top 1.2%(≈12只) | 1.2–5% | 5–10% | 10–20% | 20–50% | 50–100% |
|---|---|---|---|---|---|---|
| 实际 5 日均益 | +0.65% | +0.41% | +0.44% | +0.35% | +0.38% | +0.35% |
| 实际 20 日均益 | +2.29% | +1.54% | +1.64% | +1.47% | +1.48% | +1.32% |

每桶 5 日胜率都卡在 45~48%（均值正、中位数负的右偏），区分度集中在头部 1.2%，
P88 以下基本全平 —— 所以 `buy_score` 仍然是**换手/暴露**旋钮而不是 alpha 旋钮。
现行两条线：`buy_score 0.01354` = **P94.00**（上一版是 P84.57，2026-09-26 按要求抬到
最前 6%）、`strong_score 0.01807` = **P97.00**（沿用"最前 3% 给满仓"的语义，与旧线
0.01794 只差 0.0001，等于没动）。线上方差的成色（配对样本、费前、5 日）：全体
实际 +0.368% / 胜率 47.3%，≥买线 51,285 个观测 实际 +1.167% / 胜率 51.2%，≥强线
25,636 个 实际 +1.571% / 胜率 51.8%。候选只数：**日均 57 只过买线**（中位 40，最差
一天 2024-12-20 只剩 6 只，1.6% 的日子凑不满 12 个名额；分年最低 2023 21 / 2024 6 /
2025 8 / 2026 11）、日均 29 只过强线（22.2% 的日子凑不满 12 只强档，但强档只影响
补仓深度，不影响建仓）。旧线 0.00892 的对应值是日均 147 只、最低 24 只、从不缺候选。

带位全档实测（**下表整张是 horizon=20 那一版的分数**，100 万本金，月度评估，
成本按初始本金，同一份 `predictions.parquet`，未重训；h5 的分数下只重出了首行、
带位基准档和最后一行这三档，见文首那张三档表）。整张表都是 2026-09-26 印花税分档
**之前**的口径，档位之间小于 10pp 的差不要读成好坏（理由见「交易成本口径」末段）：

| 配置 | 累计 | Sharpe | 最大回撤 | 笔数 | 成本 | 平均仓位/只数 |
|---|---|---|---|---|---|---|
| Top-50 等权（`enabled:false`） | +30.9% | 0.22 | 43.16% | 3883 | 11.51% | 92% / 50 |
| buy 0.01 | +148.7% | 0.89 | 36.34% | 435 | 7.41% | 83% / 11.8 |
| buy 0.02 | +167.5% | 0.98 | 39.17% | 505 | 8.57% | 79% / 11.8 |
| buy 0.03（带位基准档） | +123.2% | 0.80 | 41.12% | 565 | 8.86% | 75% / 11.8 |
| buy 0.05 | +120.9% | 0.81 | 42.69% | 647 | 10.48% | 70% / 10.1 |
| buy 0.03 + sell 0.01 | +114.4% | 0.71 | 43.44% | 639 | 9.93% | 81% / 11.6 |
| buy 0.03 / 20 只（base 3%） | +131.4% | 0.75 | 45.88% | 686 | 10.10% | 89% / 15.8 |
| buy 0.03 / **周度**评估 | +10.9% | 0.04 | 43.91% | 1271 | 12.30% | 69% / 12.0 |
| — 静态压总仓位：上限 80% | +91.6% | 0.65 | 42.18% | 534 | 7.65% | 71% / 11.5 |
| — 静态压总仓位：上限 60% | +78.3% | 0.74 | 34.87% | 364 | 5.02% | 50% / 8.1 |
| — 指数跌破 MA200 强制空仓 | +29.1% | 0.29 | 19.22% | 345 | 4.07% | 75% / 6.6 |
| — 指数跌破 MA100 强制空仓 | +11.8% | 0.07 | 23.06% | 358 | 4.03% | 80% / 6.1 |
| **A+B：IC 门控 60 日 + 目标波动 10%（当时的默认档）** | **+121.6%** | **1.22** | **23.21%** | 301 | 4.29% | 36% / 5.8 |
| 只开 A：IC 门控 60 日 | +180.9% | 1.10 | 41.12% | 557 | 8.74% | 70% / 11.0 |
| 只开 B：目标波动 10% | +88.9% | 0.93 | 23.21% | 319 | 4.48% | 40% / 6.4 |
| A+B：目标波动 15% | +113.5% | 0.94 | 39.05% | 409 | 5.26% | 51% / 8.6 |
| 对照：静态上限 45%（名额 7 只） | +99.0% | 1.09 | 24.26% | 263 | 3.89% | 38% / 6.1 |
| 对照：静态上限 30%（名额 4 只） | +98.9% | 1.39 | 21.69% | 172 | 2.61% | 25% / 3.6 |

前 12 行是 `position_policy` 的档位（无暴露层），后 6 行是 `exposure_overlay`
的同口径 A/B（`research/policy_grid.py` §5，44 个月度评估）。

**同一批暴露层档位在现行口径（h5 分数 + P94 建仓线 + 次日开盘判定）下重跑**，
脚本 `research/h5_arms.py`（输出 `logs/h5_arms_p94.log`，45 个月度评估，Sharpe 取报告
口径 rf=2%，成本按初始本金）：

| 配置 | 累计 | Sharpe | 最大回撤 | 笔数 | 成本 | 平均仓位/只数 |
|---|---|---|---|---|---|---|
| Top-50 等权（`enabled:false`） | +75.21% | 0.52 | 39.23% | 3981 | 13.82% | — / — |
| 带位（无暴露层） | +115.34% | 0.70 | 43.81% | 651 | 9.97% | 78.0% / 11.9 |
| **带位 + A+B（现行默认）** | **+159.22%** | **1.62** | **22.67%** | 306 | 5.20% | 35.9% / 5.0 |
| 只开 A：IC 门控 60 日（不缩波动） | +76.79% | 0.56 | 40.89% | 604 | 7.69% | 68.5% / 10.7 |
| 只开 B：目标波动 10%（无门控） | +174.31% | 1.57 | 23.09% | 338 | 6.25% | 37.8% / 5.5 |
| 对照：静态上限 45%（名额被迫降到 7 只） | +132.64% | 1.23 | 19.99% | 321 | 5.00% | 37.5% / 5.5 |
| 对照：静态上限 30% + 名额 4 只 | +40.80% | 0.63 | 18.24% | 172 | 2.11% | 22.2% / 3.7 |

静态 45% 那一档是 B 的**同暴露对照**：平均仓位 37.5% 对 37.8%、平均只数都是 5.5，
差别只在"上限是写死的"还是"跟着波动浮动的"。（静态档必须同时降 `max_names`：
`PolicyConfig` 会校验 `max_names × base_weight ≤ (总上限 − 补仓预留)/(1+费率)`，
45% 上限只塞得下 7 只 5% 基础仓，12 个名额直接报参数矛盾。）

**目标波动在现行口径下重扫一遍**（A 门控保持开启，只改 `vol_target_ann`；这张表替掉了
h20 那一版的同名扫描）：

| 目标年化波动 | 累计 | Sharpe | 最大回撤 | 笔数 | 成本 | 平均仓位/只数 | 上限区间 |
|---|---|---|---|---|---|---|---|
| 8% | +151.34% | **1.82** | **11.75%** | 243 | 4.26% | 29.9% / 4.0 | 14%~72% |
| **10%（现行）** | **+159.22%** | 1.62 | 22.67% | 306 | 5.20% | 35.9% / 5.0 | 14%~90% |
| 12% | +147.92% | 1.39 | 26.09% | 366 | 5.92% | 40.7% / 5.8 | 14%~95% |
| 15% | +104.76% | 0.90 | 37.19% | 465 | 6.45% | 51.5% / 7.4 | 14%~95% |
| 20% | +66.08% | 0.52 | 39.28% | 563 | 7.32% | 60.4% / 9.1 | 16%~95% |

Sharpe 一路随目标下调而升（0.52→0.90→1.39→1.62→1.82），累计却在 8%~12% 之间是平的
（+147.9%~+159.2%，彼此差 7~11pp，正压在 7.0pp 噪声带边缘），回撤从 22.67% 直接砍到
11.75%。**现行 10% 不是风险调整最优，是"多要 7.9pp 累计换一倍回撤"的偏好选择**；
再往下就没有免费午餐了：B 的输入是 45 个评估日上的全池等权 20 日已实现年化波动，
中位 **20.0%**、最低 10.5%（2025-07）、最高 70.1%、均值 23.7%（与回测报告里那个 23.7%
对得上，`logs/calib_stats.log` §6）；`scale_floor=0.30` 在 8% 那一档已经顶住
**11/45** 个评估日（10% 是 6/45），再降到 6% 以下就是长期顶地板 = 退化成静态
28.5% 上限，而静态 30% + 名额 4 只实测只有 +40.80% / Sharpe 0.63。

四条结论，改代码之前先看清：

1. **分数线不控制暴露**。h20 那张表的每日过线只数：`0.01` 均 449、`0.02` 均 280、
   `0.03` 均 148（最差一天仍剩 11 只）、`0.05` 均 29（2024、2025 各有一天挂 0）。
   只有抬到 0.05 才偶尔
   真的挡在 `max_names=12` 前面，可那一档回撤 42.69%，比 0.01 档的 36.34% 更差 ——
   "没有新名字可买"不等于"把手上的卖掉"，`sell_score` 还挂在 0。把线从 0.01 抬到 0.05
   只让平均仓位 83%→70%。想压回撤只有两条路：压总仓位（上限 60% →
   34.87%）或加组合级门控（MA200 → 19.2%，代价是收益 +123%→+29%；MA100 在样本里
   翻转 20 次，把收益磨成 +11.8%）。压总仓位这一条现在做成了代码里的
   `exposure_overlay`（见上一节），趋势门控仍然只有 MA200 这种硬开关、未接入。
   （h5 现行档同一算法：买线 0.01354（P94）日均 57 只过线，896 个交易日里只有 1.6%
   的日子候选不足 12 只 —— 12 个名额仍然先于分数线用满，这条机制与标签长度、
   与线抬到哪一档都无关。）
2. **收益的提升来自集中，不来自择时**。现行口径的月度检验（`logs/calib_stats.log` §5：
   每个月末信号日取分数前 N 只等权，减当日全池等权，看随后 20 个交易日）——前 12 只
   超额**月均 +1.36%、中位 −1.58%**，43 个可算的月份里只有 **18 个月**为正，t 值 **+0.80**
   （远不到 2），累计 Σ +58.3% 里最好 3 个月占 **156%**，剔掉那三个月月均变 **−0.82%**。
   往前看前 3 只 +7.12%（为正 22/43、t=+1.30），往后看前 30 只 +0.04%、前 100 只 +0.42%：
   出了前 12 名就全平，后三个数彼此差不到半个点，而这类月度序列单月就能摆几个点
   （前 12 只那列：月度标准差 **11.11%**、中位 −1.58%、最好 3 个月占掉 156%）。5 日窗口
   同一张表，前 12 只月均 +0.47%、为正 22/45。按每日名次给全体 896 天分桶也是同一个形状
   （上面那张分桶表，`research/h5_dist.py`）：top 1.2% 五日 +0.65%，5–10% +0.44%，
   50–100% +0.35%。
   组合层面的贡献同样偏：现行档分年 2023 +1.2% / 2024 +60.5% / 2025 +26.7% / 2026 +25.9%
   （`reports/verify_results.txt`，指数同期 −13.5% / +1.2% / +27.5% / +2.2%；口径 = 年内日收益
   复利，四行相乘正好回到全期 +159.22%）——
   单年最高 2024 占 +60.5%，其余三年在 +1%~+27% 之间，只有 2025 跑输指数（差 0.8 个点，
   跑输等权组合 14.9 个点）。
   h20 那一版的对应检验（Top-12 减 Top-50 月差 +0.24%、44 个月里 18 个月为正）方向一致，
   只是头部更薄。
3. **评估频率要贴着标签**（只在 h20 上实测过）。周度评估把 Sharpe 打到 0.04（565 笔
   →1271 笔，成本 12.3%）：20 日收益率的分数一周内基本没动，多看几次只是多付一次
   换手。**换成 5 日标签后月内节奏没有重测**，现行仍是月度评估 —— 分数现在动得更快，
   月度是"够了"还是"太慢"是个还没回答的问题。
4. **暴露层里真正在做事的是 B，A 单独开着是负贡献**。归因表在上面
   （`research/h5_arms.py` → `logs/h5_arms_p94.log`），四点摊开：
   (a) **A+B 相对"只开带位"**：Sharpe 0.70→**1.62**、回撤 43.81%→**22.67%**、
       成本 9.97%→5.20%，累计 +115.34%→**+159.22%**，代价是平均仓位 78.0%→35.9%、
       持仓 11.9→5.0 只（B 给的平均上限 44.8%，扣掉 `add_reserve_weight` 预留的 5%
       之后一轮只塞得下 7 只 5% 基础仓）。h20 那一版的对应结论是"累计几乎不变
       （+123.2%→+121.6%）、只有风险变好"，换到 5 日标签后连绝对收益都一起变好了。
   (b) **B 单独就够，A 目前是累赘**：只开 B 是 +174.31% / Sharpe 1.57 / 回撤 23.09%，
       比现行 A+B 多 **+15.1pp 累计**、回撤只多 0.42pp、Sharpe 只差 0.05（15.1pp 出
       这一族的 7.0pp 噪声带，0.05 的 Sharpe 差在带内，不能读）。只开 A（不缩波动）
       是 +76.79% / 0.56 / 40.89%，**比完全不加暴露层还少 38.6pp**，只换来回撤从
       43.81% 压到 40.89%。A 触发 8 次（2023-05、2024-01~02、2026-05~09），8 次里
       5 次落在最近 5 个月；h20 那一版"只开 A +180.9%"整个来自样本最后 13 个月
       （正是模型失效段），换到 5 日标签这个来源就没了。结论：**A 没有独立价值**，
       留在默认档只为压"信号已失效却还满仓"这一类状态，而这段样本里没真出现过。
       要去掉它是一行配置 `exposure_overlay.ic_window_days: 0`，不需要重训 ——
       本轮**没有**改，等定夺。
   (c) **"B 到底买到了什么"结案**：静态上限 45%（同平均仓位 37.5% vs 37.8%、同 5.5 只、
       成本 5.00% vs 6.25%）只有 +132.64% / Sharpe 1.23，B 比它多 **+41.7pp**、
       Sharpe +0.34，代价是回撤多 3.10pp —— B 买的是"上限跟着波动浮动"（样本里
       14%~90%），静态档给不了。但再往下压不划算：静态 30% + 名额 4 只只有
       +40.80% / Sharpe 0.63，h20 那一版"30%/4 只 Sharpe 1.39 反而更高"在现行口径下
       **不成立**，那条是单档样本内现象，"回撤主要由暴露决定"这条到此见底。
   (d) **B 的水平本身也没扫到底**：现行口径重扫目标波动（上面第二张表），Sharpe 随目标
       下调单调升到 8%（1.82）、回撤砍半（11.75%），而 8%~12% 的累计基本平坦。
       10%→8% 是"少 7.9pp 累计换一半回撤"的偏好题，本轮**没改**，等定夺；地板
       `scale_floor=0.30` 在 8% 那一档已经顶住 11/45 个评估日，再往下就不是同一个旋钮
       在起作用了。


## 回测有没有用未来数据（2026-09-25 首审，2026-09-26 换成交判定/建仓线后重跑）

判据只有一条：**把某个切点之后的数据毁掉，切点之前的一切必须逐格不变**。切点取
2025-06-30，可复现脚本 `research/no_lookahead.py`（输出 `logs/no_lookahead_p94.log`，
末行是记账对账，不是前视探针）。

| 环节 | 扰动 | 结果 |
|---|---|---|
| 撮合与盯市 | 切点之后的价格 ×0.5 / 整段砍掉，各跑一遍全量回测 | 切点之前 585 天净值最大差 **0.00e+00 元**、成交流水差 **0 笔**；期末净值分别掉到 1.848× / 1.767×（扰动确实生效） |
| 技术因子 | 只用切点之前的 K 线重算，与全样本值逐格比对 | 抽样 60 只（切点前 ≥120 根）× 1,616,160 单元格，**不一致 0 格** |
| walk-forward 标签 | 数落在训练窗口边界重叠区里的月度信号日 | horizon=5：**0/44** 个信号日落进重叠区（horizon=20 是 8 个）—— 短标签顺带消掉了这类边界污染 |
| 按笔盈亏口径 | Σ每笔已实现盈亏 vs 净值变动 | +1,621,364 元 vs +1,621,364 元，**倒挤出的浮动盈亏 0 元**（现行档期末空仓，期末市值 0 元）—— 上一版口径期末还剩 56,526 元浮盈，这次是逐分对平；旧口径"卖额−卖出费"给的是 17,178,527 元 = 10.6× 净值变动，那是现金流不是盈亏 |

结构上防前视的四处，读代码时值得先看：因子一律 lag 1 期、处理只按 `groupby("date")`
做截面运算；`close.shift(-h)/close` 这个标签只进训练目标，绝不进特征；信号日收盘决定
→ 次日开盘成交（涨停不买、跌停不卖、停牌不成交）；暴露层只取"观测日 ≤ 评估日 − h"
的已兑现 RankIC。基本面默认关闭（`use_spot_data: false`）也是同一条理由——实时快照
平铺到历史日期就是未来函数。

一处不影响前视但值得知道的粗糙：`factor_panel` 的行序并不按日期单调，所以 `cmd_train`
里"取训练集尾部 15% 做 early stopping"那一刀切到的不是时间上最后的 15%。未来的数据
仍然进不去（`date < 窗口起点` 的过滤在切片之前），但早停用的验证片段是散布在各期的。

## 交易成本口径（报的都是费后，各项各收多少）

`python main.py backtest` 打的每一个收益数字都已经扣掉佣金、印花税、滑点。逐笔复核
脚本 `research/cost_audit.py`（2026-09-26 在现行档重跑，输出 `logs/cost_audit_p94.log`，
约 1 分钟）：现行默认档 306 笔流水记入 **51,995 元**，与脚本独立重算的**最大逐笔差
0.000000 元** —— 少一项就是"算了没扣"，多一项就是重复计提，两边逐笔对得上才说明钱真的扣掉了。

| 项 | 费率（`config.yaml` 的 `market`） | 方向 | 本档实收 |
|---|---|---|---|
| 佣金 | 万三，单笔最低 5 元 | 双边 | 9,830 元（含 4 笔小卖出撞下限多出的 5.4 元） |
| 印花税 | 万五，**2023-08-28 前万十，按成交日取档** | 仅卖出 | 9,418 元（分档多收 813 元） |
| 滑点 | 千一 | 双边 | 32,747 元（占总摩擦 63%） |
| 合计 | 买入 13.00‱ / 卖出 18.00‱（名义） | — | 51,995 元 = 本金 5.20% |

- **滑点按成交额计提现金**（引擎记账是 `现金 -= amount + cost`），不是把成交价抬高千一。
  两者代数等价（`cost = 率 × 数量 × 价格`），只在 100 股整手上差别：折成股数是
  中位 0.045 手、最大 0.598 手，**130 笔买入里没有一笔够换一手**，所以两种口径
  逐笔股数相同。
- 买入实收 13.00‱ 与名义一致；卖出 18.48‱ 略高于名义 18.00‱，因为 4 笔小单撞上 5 元
  佣金下限（下限只咬得到 16,667 元以下的单，本档最小卖出单 10,842 元）。
  **同一原因**，下单前的资金预留必须逐笔算费用：只按费率比例留钱，会放行买不下的
  单子（`utils/sizing.py` 的 `cost_fn`）。
- 印花税分档：跨档前（2023-08-28 之前）卖出 24 笔 / 1,625,724 元，比"整段按现行档"
  多收 **813 元**（总成本 1.6%、本金 0.081%）。不分档是 2026-09-26 之前的一处真错。
  模拟盘/实盘不读这张表 —— 它们只交易"今天"，用现行档。
- 分年成本占初始本金：2023 1.03% / 2024 0.89% / 2025 2.10% / 2026 1.17%。千一固定
  滑点不含冲击成本，在这个量级（中位单笔 9.7 万元 ≈ 个股日成交额 0.07%）是偏保守的
  常数；本金上到千万级必须换成随成交量放大的冲击成本模型。

**读任何档位对照之前先记住这条**：费率一改，成交股数就改，成交股数一改，后面整条
净值路径都改。同一套"只免掉一项费率"的探针，在两档上分别测出期末净值跨度
**17.4pp**（09-25 档，笔数 278→286）和 **7.0pp**（现行档，笔数 304→307）——
省下的钱只有 0.9%~3.2% 本金，净值却能摆出十几倍。现行档"费率全零"累计 160.79%，
比费后的 159.22% 高 **1.57pp**，而名义成本合计是 5.20% 本金。所以**小于 10pp 的档位差
不能读成"更好/更差"**，它只说明这一档在头几天恰好买到了/没买到。两档都改完的
A0→A3 是 +17.1pp、三档表相邻两行相差 90pp 以上，在噪声地板之上，能读；
"只抬建仓线"那一档 −3.2pp 落在噪声里，不能读成"抬线有害"。

## 命令参考

```bash
python main.py download      # 下载数据
python main.py factors        # 计算因子 + 评估
python main.py train          # 训练模型（产出 predictions.parquet，最慢）
python main.py backtest       # 运行回测 + 生成报告
python main.py paper-trade    # 启动模拟盘
python main.py pipeline       # 一键全流程
python live/run_daily.py      # 每日链：更新→信号→调仓指令文件（不下单）
python -m pytest -q           # 108 项测试
```

改参数后该重跑到哪一步：仓位规则（`position_policy`/`exposure_overlay`/`backtest`）
只重跑 `backtest`；改 `factors`/`model.horizon` 要重跑 `train`；改 `universe`/日期才
重跑 `download`+`factors`+`train`。

## 目录结构

```
main.py            唯一研究入口（download/factors/train/backtest/paper-trade/report/pipeline）
config.yaml        所有可调参数，注释里带每个数字的来历
live/              每日链：run_daily.py 出指令，--simulate 纸面撮合，--qmt 走真实柜台
backtest/          撮合引擎 + 绩效指标 + HTML 报告
factors/           技术因子、截面处理（缩尾/标准化/中性化）、因子评估
models/            LightGBM 训练与预测（walk-forward）
utils/             position_policy.py（分数带位）、exposure.py（暴露层）、市场规则/日历
paper_trade/       模拟券商、持仓、成交日志
tests/             108 项测试（pytest -q）
research/          可复现的分析脚本，README 的表格出自这里：
                   calib_stats.py（分位点/每日过线只数/过线成色/最新截面/顶端集中度/
                   已实现波动分布，§1~§6）、policy_grid.py（阈值标定+全档实测，h20 旧表）、
                   fill_and_threshold.py（成交判定与建仓线逐变量对照）、
                   h5_arms.py（三档对照 + A/B 归因 + 静态上限对照 + 目标波动扫描）、
                   h5_dist.py（分数分桶）、verify_backtest.py（成本/超额复核）、
                   cost_audit.py（逐笔费用恒等式 + 免费率对照臂）、no_lookahead.py（前视审计）、
                   survivorship_probe.py（退市股对照 = 幸存者偏差量级）
research/archive/  依赖本机 data/cache/_c_backup/ 的历史实验，换机器跑不出来，仅供翻账
legacy/            旧每日链（menu.py/daily_runner.py/portfolio_manager.py），
                   Top-K 等权摊派，不实现分数带位；策略开启时 rebalance() 直接抛错
docs/              入门.md（第一次跑通 + 名词对照 + 七个坑）、entry-位置管理.md（仓位规则逐条来历）、
                   model-LightGBM.md（模型参考：特征/超参/逐折 RankIC/分数分布/特征重要性）
启动.bat           Windows 双击菜单，等价于上面那几条 main.py 命令
data/cache/        日线/因子/预测缓存（不进仓库，可由 main.py 重建）
reports/ logs/      回测报告与运行日志（不进仓库）
evidence/          旧引擎留下的小份存档（daily_ic 序列、verify_results 切片），只读
```

## 配置

编辑 `config.yaml` 修改：

- 数据下载（`download:`：并发线程数、全局请求速率上限、单次 HTTP 超时）
- 交易成本（佣金、印花税、滑点）
- 因子参数（周期、处理方法）
- 模型参数（LightGBM、调参）
- 回测参数（初始资金、调仓频率、持仓数）

## 要求

- Python 3.11+
- Windows / macOS / Linux
